const IRouteDALMethods = {
  setNew: async (routeObj, csvDataFile='') =>{},
  search: async (destinations, csvDataFile ='') => {},
  delete: async (routeObj, csvDataFile='') => {},
  getAll: async (csvDataFile) => {}
}

module.exports = IRouteDALMethods
